<section  id="section-{{ section.id }}" class="breadcrumb-Alignment-{{ section.settings.alignment }}">
  {% render 'breadcrumbs' %}
</section>


<style>
/* #section-{{ section.id }} {
  max-width: {{ section.settings.section_width | append: "px" }};
  margin: 0 auto;
} */
  #section-{{ section.id }} .breadcrumbs {
    font-weight: 400;
    line-height: 1.2;
    margin-left: auto;
    margin-right: auto;
    margin-top: 1rem;
    max-width: 180rem;
   margin-bottom: 1rem;
}
  .breadcrumb-Alignment-right .breadcrumbs .breadcrumbs__list {
    justify-content: flex-start;
}
  .breadcrumb-Alignment-center .breadcrumbs .breadcrumbs__list {
    justify-content: center;
}
  .breadcrumb-Alignment-right .breadcrumbs .breadcrumbs__list {
    justify-content: flex-end;
}
</style>

{% schema %}
  {
    "name": "Breadcrumb",
    "settings": [
    ],
    "presets": [
      {
          "category": "header",
          "name": "Breadcrumb",
          "settings": {}
        }
      ]
  }
{% endschema %}
